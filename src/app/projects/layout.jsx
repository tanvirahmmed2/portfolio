import React from 'react'

export const metadata={
    title:'Tanvir Ahmmed | Projects',
    description: "Latest updated projects"
}

const Layout = async({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default Layout
