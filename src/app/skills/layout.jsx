
export const metadata={
    title:'Tanvir Ahmmed | Skills',
    description:'Here all skills are decorated'
}

const Layout = ({children}) => {
  return (
    <div>
      {children}
    </div>
  )
}

export default Layout
